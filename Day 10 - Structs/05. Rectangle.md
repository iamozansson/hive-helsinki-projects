# Mandatory 2 / 2

1

## Rectangle

**Learning Objectives:**

* Define and use multiple data structures.
* Read data from one structure to populate another.

### Instructions

Create a structure called `Coords` with the following fields (in the exact order):

```text id="x8p4mz"
X - integer
Y - integer
```

Create a structure called `Rectangle` with the following fields (in the exact order):

```text id="v2k7qd"
Width - integer
Height - integer
Area - integer
Perimeter - integer
```

Write a function that takes two `Coords` structures representing the bottom-left and top-right corners of a rectangle on a coordinate plane, and returns a `Rectangle` with all fields filled in correctly.

#### Expected function/structure signature

```go id="n6t3wr"
type ??? // Fill these in

func GetRectangle(min, max Coords) Rectangle {

}
```

#### Usage

```go id="p9c5ks"
min := Coords{X: 1, Y: 1}
max := Coords{X: 6, Y: 4}

GetRectangle(min, max)
>> {5, 3, 15, 16}
```
